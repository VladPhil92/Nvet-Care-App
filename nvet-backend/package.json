{
  "name": "nvet-backend-railway-compat",
  "version": "0.0.0-p0-bridge",
  "private": true,
  "description": "Temporary Railway compatibility bridge. Canonical backend source is fetched from the exact deployment SHA.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "prisma:generate": "node bridge.mjs prisma-generate",
    "build": "node bridge.mjs build",
    "start": "npm run start:prod",
    "start:prod": "npm run deploy:preflight && node runtime-smoke.mjs",
    "deploy:preflight": "node bridge.mjs preflight",
    "prisma:migrate:prod": "node bridge.mjs preflight"
  },
  "dependencies": {
    "tar": "^7.4.3"
  }
}
