name: Phase 21 Format Fix

on:
  push:
    branches:
      - feat/vet-invitation-conversion-phase21

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: feat/vet-invitation-conversion-phase21
      - uses: actions/setup-node@v7
        with:
          node-version: 22.x
          cache: npm
          cache-dependency-path: package-lock.json
      - run: npm ci --include=dev --legacy-peer-deps
      - name: Format Phase 21 backend files
        run: npx prettier --write backend/src/recruitment/dto/vet-invitation.dto.ts backend/src/recruitment/vet-invitation.service.ts
      - name: Commit formatting if needed
        run: |
          if git diff --quiet; then
            echo "No formatting changes required."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add backend/src/recruitment/dto/vet-invitation.dto.ts backend/src/recruitment/vet-invitation.service.ts
          git commit -m "style(recruitment): apply Phase 21 prettier formatting"
          git push origin HEAD:feat/vet-invitation-conversion-phase21
