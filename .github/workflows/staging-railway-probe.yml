name: Staging Railway Credential Probe

on:
  push:
    branches:
      - ops/staging-railway-probe-20260830
    paths:
      - '.github/workflows/staging-railway-probe.yml'

permissions:
  contents: read

jobs:
  probe:
    name: Probe Railway workspace read-only
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      RAILWAY_API_TOKEN: ${{ secrets.RAILWAY_API_TOKEN }}
    steps:
      - name: Verify credential presence
        run: |
          set -euo pipefail
          if [ -z "$RAILWAY_API_TOKEN" ]; then
            echo '::error::RAILWAY_API_TOKEN is not configured as a repository secret.'
            exit 78
          fi
          echo 'RAILWAY_API_TOKEN is configured.'

      - name: Install Railway CLI
        run: npm install --global @railway/cli

      - name: List accessible Railway projects
        run: |
          set -euo pipefail
          railway project list --json > /tmp/railway-projects.json
          jq -r '.[] | "PROJECT name=\(.name) id=\(.id)"' /tmp/railway-projects.json
          count="$(jq 'length' /tmp/railway-projects.json)"
          echo "Accessible project count: $count"
          if [ "$count" -eq 0 ]; then
            echo '::error::The workspace token exposes no Railway projects.'
            exit 1
          fi

      - name: Check canonical Nvet project visibility
        env:
          TARGET_PROJECT_ID: 61c97ce7-fd4d-4d8d-9e28-8c0ef964d8cd
        run: |
          set -euo pipefail
          if jq -e --arg id "$TARGET_PROJECT_ID" '.[] | select(.id == $id)' /tmp/railway-projects.json >/dev/null; then
            echo 'Canonical Nvet Railway project is visible to this token.'
          else
            echo '::error::Canonical Nvet Railway project is NOT visible to this workspace token.'
            exit 2
          fi

      - name: Publish probe summary
        if: always()
        run: |
          {
            echo '## Railway read-only workspace probe'
            echo ''
            echo '- Credential: present.'
            echo '- No Railway project, environment, service, variable, deployment, or database was changed.'
            if [ -f /tmp/railway-projects.json ]; then
              echo '- Accessible projects:'
              jq -r '.[] | "  - `\(.name)` — `\(.id)`"' /tmp/railway-projects.json
            fi
          } >> "$GITHUB_STEP_SUMMARY"
